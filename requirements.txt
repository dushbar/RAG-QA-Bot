# Core RAG pipeline
chromadb>=0.5.0            # local, persistent vector store (no server to manage)
sentence-transformers>=2.7.0  # local embeddings -> ingestion is free, no API key needed

# Document loaders
pypdf>=4.2.0
python-docx>=1.1.2

# LLM providers (only the one you configure in .env needs a valid key)
anthropic>=0.34.0
openai>=1.30.0

# App / utils
streamlit>=1.36.0
python-dotenv>=1.0.1
